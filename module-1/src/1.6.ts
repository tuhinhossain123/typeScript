{
  //

  // method

  const users = {
    name: "Tuhin Hossain",
    age: 23,
    balance: 2020,

    addBalance(balance: number): string {
      return `user balance is  ${this.balance + balance}`;
    },
  };

  //
}
